security:
  oauth2:
    client:
      clientId: hello-service
      clientSecret: ${HELLO_SERVICE_PASSWORD}
      accessTokenUri: http://authorization-service:1001/uaa/oauth/token
      grant-type: client_credentials
      scope: server

server:
  context-path: /hello
  port: 1002