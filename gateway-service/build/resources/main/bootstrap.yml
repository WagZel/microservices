spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://configuration-service:1000
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user